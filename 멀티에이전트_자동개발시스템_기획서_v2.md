# 멀티 에이전트 기반 자동화 소프트웨어 개발 시스템 구축 기획서 (개정판)

## 1. 프로젝트 개요

본 기획서는 기획, 개발, 코드 리뷰, GUI 검증으로 역할을 분담한 다중 AI 에이전트(Multi-Agent System)가 협력하여 웹 기반 프론트엔드 애플리케이션을 자율적으로 구축하는 파이프라인 설계안이다.

에이전트가 실제 사용자처럼 화면을 시각적으로 인지하고 마우스를 제어하며 동적으로 검증하는 과정을 시연하여, 코딩 자동화를 넘어선 Vision 기반 에이전틱(Agentic) 시스템을 구현하는 것을 목표로 한다.

기존 기획 대비 개정된 핵심 사항:
- 정적 문법 검사(Lint)는 개발자 에이전트의 내부 툴로 흡수하고, 의미적 코드 리뷰만 별도 에이전트로 분리
- 기획자 에이전트에 Phase별 성공 조건 정의 기능과, 반복 실패 시 재계획 및 사람 에스컬레이션 로직 추가
- GUI 검증 에이전트는 Set-of-marks 기반 Vision 판단으로 설계하여 비용과 정확도를 함께 확보

## 2. 에이전트 구성 및 역할 분류

에이전트 여부는 "자율적 판단이 필요한가"를 기준으로 구분한다. 결정론적 검사는 툴로, 의미적 판단이 필요한 작업만 별도 에이전트로 둔다.

| 컴포넌트 | 유형 | 역할 |
|---|---|---|
| 기획자 (Planner) | 에이전트 | 요구사항 분할, 성공 조건 정의, 실패 시 재계획 및 에스컬레이션 |
| 개발자 (Developer) | 에이전트 | 코드 작성, 로컬 빌드/실행, 내장 Lint 툴 활용 |
| 코드 리뷰어 (Reviewer) | 에이전트 | 로직/요구사항 부합 여부에 대한 의미적 검토 |
| GUI 검증 (GUI Tester) | 에이전트 | Vision 기반 화면 인지 및 물리적 조작 시뮬레이션 |
| Lint/정적 검사 | 툴 (개발자 에이전트 내장) | 문법·타입 오류 등 결정론적 검사 |

### 2.1 기획자 에이전트 (Planner)

**역할**
- 초기 요구사항을 마이크로 단위 Phase로 분할
- 각 Phase마다 명시적 성공 조건(예: "로그인 버튼 클릭 시 /dashboard로 이동하고 화면에 '환영합니다' 텍스트가 노출되어야 함")을 함께 정의하여 개발자 및 검증 에이전트에게 전달
- State Tracker로 전체 파이프라인 진행 상태 추적
- 개발자↔검증 루프가 정해진 횟수(3~5회) 반복 실패할 경우, 동일한 접근을 반복시키지 않고 **재계획**을 수행: 접근 방식 변경, 기능 범위 축소 등 전략 자체를 조정
- 재계획 후에도 해결되지 않으면 **사람에게 검증을 요청**하고, 사람의 피드백을 반영해 새 계획으로 루프를 재개

**도구**: JSON/Markdown 문서 생성기, 파이프라인 상태 추적기(State Tracker)

**설계 노트**: 반복 실패 시 사람을 부르는 것은 자율성의 결여가 아니라, "같은 방식을 반복해도 소용없다"는 판단에 따른 자율적 에스컬레이션으로 간주한다. 사람 개입 후에도 재개 로직(새 계획 전달 → 개발자 재착수)이 자동으로 이어지도록 설계한다.

### 2.2 개발자 에이전트 (Developer)

**역할**
- 기획안에 맞춰 코드를 작성하고 로컬 환경에서 빌드 및 실행
- 문법 오류, 타입 불일치, import 누락 등은 내장 Lint/AST 툴을 직접 호출하여 스스로 확인 후 즉시 수정 (별도 에이전트 통신 없이 처리하여 호출 비용 절감)
- 실행 단계에 타임아웃, 재시도, 명확한 성공 판정 기준(포트 리스닝 확인, 헬스체크 등)을 적용하여 환경 이슈로 인한 파이프라인 중단을 방지

**도구**: 파일 시스템 입출력(I/O) 제어기, CLI 및 로컬 서버 구동 환경, Linter/AST 파서

### 2.3 코드 리뷰어 에이전트 (Reviewer)

**역할**
- 개발자 에이전트가 작성한 코드에 대해 문법이 아닌 **의미적 정합성**을 검토: 로직 오류, 기획자가 정의한 Phase별 성공 조건과의 부합 여부
- 개발자와 분리된 컨텍스트에서 코드를 검토함으로써, 작성자 스스로는 발견하기 어려운 논리적 맹점을 잡아내는 것을 목적으로 함
- 피드백 루프에는 재시도 상한을 두어 리뷰어-개발자 간 무한 핑퐁을 방지

**도구**: 코드 정적 분석 결과 및 기획자가 제공한 성공 조건 문서

### 2.4 GUI 검증 에이전트 (GUI Action Tester)

**역할**: 렌더링된 화면을 Vision으로 인지하고, 실제 마우스 커서 이동 및 키보드 입력을 시뮬레이션하여 UI/UX를 검증

**설계 방식**: 판단은 Vision 모델이 수행하되, 아래 구조로 비용과 정확도를 함께 확보한다.

- **Set-of-marks 방식**: 화면에서 클릭 가능한 요소를 자동 탐지(윤곽 탐지 또는 OCR)하여 번호 라벨을 오버레이한 이미지를 Vision 모델에 입력. 모델은 좌표 대신 "N번 요소" 형태로 판단하여 좌표 추측 오차를 구조적으로 제거
- **호출 최소화**: 매 클릭마다 스크린샷을 입력하지 않고, 화면 상태가 실제로 전환됐을 때만 새 스크린샷을 입력. 관심 영역만 크롭/다운스케일하여 토큰 비용 절감
- **액션 결정 통합**: "다음 행동 + 대상 요소"를 한 번의 호출로 판단하여 왕복 횟수 최소화
- **실패 감지 및 재시도**: 액션 수행 후 재스크린샷으로 기대한 화면 전환 여부를 재확인하고, 실패 시 재시도

**도구**: 화면 캡처 및 요소 탐지기(Set-of-marks 전처리), Vision 모델, OS 레벨 키보드/마우스 제어 라이브러리

## 3. 단계별 개발 및 검증 워크플로우

1. **기획 및 태스크 분할**: 기획자 에이전트가 요구사항을 모듈 단위 Phase로 분리하고, 각 Phase의 성공 조건을 함께 정의하여 개발자에게 전달
2. **코드 작성 및 내장 정적 검사**: 개발자 에이전트가 코드를 작성하며 Lint/AST 툴로 문법 오류를 즉시 자체 수정
3. **의미적 코드 리뷰**: 코드 리뷰어 에이전트가 로직 및 성공 조건 부합 여부를 검토, 문제 발견 시 개발자에게 반환 (상한 재시도)
4. **동적 GUI 상호작용 검증**: 리뷰를 통과한 코드가 화면에 렌더링되면 GUI 검증 에이전트가 Set-of-marks 기반 Vision 판단으로 클릭·입력 시나리오를 수행하고 기대 결과와 대조
5. **에러 피드백 루프**: GUI 검증 실패 시 증상과 대상 요소 정보를 개발자에게 반환하여 재수정 지시 (상한 3~5회)
6. **재계획 및 에스컬레이션**: 4~5단계 루프가 상한 횟수만큼 반복되어도 해결되지 않으면 기획자 에이전트가 재계획을 수행하고, 그래도 해결되지 않으면 사람에게 검증을 요청. 사람의 피드백을 반영한 새 계획으로 루프 재개

## 4. 시스템 시연 시각화 방안

- **로컬 렌더링 환경 노출**: 개발자 에이전트가 빌드한 웹 앱이 실제 브라우저 창으로 열리는 과정을 노출
- **상호작용 궤적 시각화**: GUI 검증 단계에서 Vision 모델이 판단한 대상 요소로 마우스 커서가 이동하는 궤적과 키보드 입력 과정을 실시간으로 노출
- **데모 안정성 확보**: 라이브 데모를 우선 시도하되, 사전에 동일 시나리오를 반복 실행하여 성공률을 파악해두고, 실패 상황에 대비해 사전 녹화된 성공 사례를 백업으로 준비

## 5. 최종 산출물

모든 Phase의 검증(정적 검사, 코드 리뷰, GUI 검증)이 완료되면, 기획자 에이전트가 전체 에러 발생 내역, 재계획 이력, 사람 개입 이력이 기록된 개발 요약 보고서를 작성하고 최종 애플리케이션 소스 코드와 함께 제출한다.

## 6. 남은 설계 과제

1. 코드 리뷰어의 검토 기준을 기획자가 정의한 성공 조건 문서와 연동하는 구체적 인터페이스 설계
2. GUI 검증의 Set-of-marks 탐지 방식 프로토타이핑 (윤곽 탐지 vs OCR 비교)
3. 재계획과 단순 재시도를 구분하는 프롬프트/전략 변경 로직 구체화
4. 전역 재시도 상한 및 사람 에스컬레이션 트리거 조건의 수치 확정
5. 데모 시나리오별 사전 성공률 측정 및 백업 영상 준비
